{
  "dependencies": {
    "@tensorflow-models/universal-sentence-encoder": "^1.3.3",
    "@tensorflow/tfjs": "^4.7.0",
    "@types/chrome": "^0.0.237",
    "babel-loader": "^9.1.2",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "tsc --outDir ./extension_dist ./extension_src/*.ts && sleep 5 && node esbuild.js",
    "test": "node -v && tsc --version"
  },
  "devDependencies": {
    "esbuild": "^0.17.19"
  }
}
